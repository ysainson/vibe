{
  "name": "vibe-tools",
  "private": true,
  "type": "module",
  "description": "Dev + release tooling for the VIBE marketplace. Bun always.",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "release": "bun tools/release.ts",
    "pins": "bun tools/pins.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  },
  "dependencies": {
    "@clack/prompts": "^1.6.0"
  }
}
